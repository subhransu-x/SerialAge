# Cloudflare Pages static site redirects
#
# No SPA fallback is needed because this site is fully SSG.
# Cloudflare Pages automatically serves /404.html with a 404 status code
# when no file matches the requested path.
