# Development site URL — used for canonical tags in local builds.
# Canonical tags will resolve to this during `npm run dev`.
# The production build overrides this with .env.production.
VITE_SITE_URL=https://serialage.com
